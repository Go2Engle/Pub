## Summary

Describe the change briefly.

## Release Notes

- Use a Conventional Commit title so squash merges produce release-friendly history.
- Examples:
  - `feat: add package search`
  - `fix(ui): preserve selection after refresh`
  - `chore(ci): update release automation`
- Prefer squash merge so `main` stays linear and Release Please sees one clean commit subject.
