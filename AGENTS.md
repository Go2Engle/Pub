# Repository Instructions

- Use Conventional Commit messages for every commit in this repository.
- Prefer squash merges so the commit subject on `main` matches the pull request title.
- For release-relevant changes, use `feat:`, `fix:`, or `deps:` so Release Please can pick them up.
